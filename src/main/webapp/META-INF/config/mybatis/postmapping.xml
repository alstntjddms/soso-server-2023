<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.soso_server.tmp.Board.IPostDAO">

    <select id="findAll" resultType="PostDTO">
        SELECT * FROM TEST.post ORDER BY NUM ASC
    </select>

    <insert id="insert" parameterType="PostDTO">
        INSERT INTO TEST.POST(NUM, TITLE, CATEGORYNUM, WRITEBY, TEXT, WRITEAT)
        VALUES (#{num}, #{title}, #{categorynum}, #{writeBy}, #{text}, CURRENT_TIMESTAMP)
    </insert>

    <select id="insertResult" resultType="Integer">
        SELECT MAX(NUM) FROM TEST.POST
    </select>
    
    <delete id="deleteComment" parameterType="Integer">
        DELETE FROM TEST.comment WHERE NUM =  #{postNum}
    </delete>

    <update id="update" parameterType="java.util.Map" >
        UPDATE TEST.post SET title=#{title}, categorynum=#{categorynum}, writeBy=#{writeBy}, text=#{text}, writeAt=CURRENT_TIMESTAMP WHERE NUM = #{num}
    </update>

    <delete id="deletePost" parameterType="Integer">
        DELETE FROM TEST.post WHERE NUM =  #{postNum};
    </delete>

    <select id="findOne" parameterType="Integer" resultType="PostDTO">
        SELECT * FROM TEST.post WHERE NUM = #{postNum}
    </select>

    <insert id="insertComment" parameterType="java.util.Map">
        INSERT INTO TEST.POST(NUM, COMMENT, COMMENTAT)
        VALUES (#{num}, #{comment}, #{commentAt})
    </insert>

    <select id="findList" parameterType="Integer" resultType="PostDTO">
        SELECT * FROM TEST.POST ORDER BY NUM LIMIT 3 OFFSET ${index}
    </select>

</mapper>
